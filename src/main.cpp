#include <Arduino.h>
#include "AlgLib.h"

void setup() {
  // put your setup code here, to run once:
  char str[10];
  int mano;
   SeleCombo(1,str);
  printf("se, %s", str);
  scanf("bith %d", &mano);

}

void loop() {

  

 
  // put your main code here, to run repeatedly:
}